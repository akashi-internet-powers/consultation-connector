# CLAUDE.md — consultation-connector

このリポジトリは、各団体(NPO法人本体、法人内の別事業、将来の他法人)のWordPressに導入する、
相談会日程を管理・配信するためのプラグインです。命名は「各団体のWordPressを通知ネットワークに
接続する」役割に由来します(`consultation-app` / `consultation-connector` / `consultation-hub` の3リポジトリ構成)。

## 位置づけ

- **これは配布物。** 自団体だけでなく、将来的に他法人にも「プラグインを入れるだけで参加できる」ことを
  目的とした設計にする。団体固有のロジックを埋め込まない。
- consultation-app(アプリ側)がこのプラグインのREST APIを叩いて日程を取得する。
- 予約データはこのプラグインでは扱わない。日程には「外部予約システムへのURL」のみを持たせる。

## 主な機能

1. カスタム投稿タイプ `consultation_event` の登録(`show_in_rest: true`)
2. カスタムフィールド: 開催日・並び順 / 時間帯表示(自由記述) / 場所の名称 / 住所 / ステータス(受付中・満席・終了) / 予約URL
3. REST APIでの日程一覧・詳細の公開(標準の `wp/v2/consultation_event` エンドポイントを利用)

## スコープ外(意図的に持たない機能)

- 予約フォーム・予約データの保持
- プッシュ通知の送信処理そのもの(中央管理側 `consultation-hub` の責務)
- QRコード生成(初回リリースでは対応しない。将来拡張として想定)
- 定員のシステム連携・自動満席判定(主催者による手動運用)

## カスタムフィールドの実装方針(決定事項)

ACFは使わない。`register_post_meta` + 自作のメタボックス(`includes/class-meta-box.php`)で実装する。
理由:
- フィールドが単純(日付・自由記述・場所・URL・選択肢のみ)で、ACFの繰り返しフィールドや条件分岐が不要
- 他法人への配布物であるため、ACFという外部依存を導入団体に強制したくない
- 将来、繰り返しフィールドや複雑な条件分岐が必要になった場合のみ再検討する

## 開催日時と複数時間枠の扱い(決定事項)

1つの相談会に複数の時間枠(例: 10:00〜/13:00〜/15:00〜)があり得るが、**同一イベント(同一投稿)として扱い、
時間帯は`time_note`フィールドへの自由記述で表現する。** 繰り返しフィールドによる厳密な枠管理は行わない。
`start_at`の時刻部分は同日内の表示順を決めるためだけに使用し、公開画面には表示しない。公開画面の時間帯は`time_note`のみを表示する。
予約自体は本システムのスコープ外(既存の予約システムに委譲)であるため、時間枠を構造化するモチベーションが
そもそも薄いという判断による。

## ステータス(受付中/満席/終了)の持ち方(決定事項)

投稿ステータス(`register_post_status`)は使わず、カスタムフィールド(`status`メタ)のまま実装する。
理由:
- 相談会日程は満席・終了後も一覧やアプリ上で表示し続ける必要があり、「常に公開されたまま」という
  前提と、WordPressの`post_status`が本来表す「公開されているか否か」という軸が噛み合わない
- 投稿ステータスを流用すると、管理画面UI(公開ボックスへの独自ステータス表示)やREST APIの
  既定の公開範囲(publish以外を匿名リクエストに返さない挙動)を個別に上書きする作業が発生し、
  かえってWordPress標準から離れた実装になる
- カスタムフィールドのままの方が、標準機能に対して素直な実装になる

## 定員管理の扱い(決定事項)

定員の自動判定・システム連携は行わない。主催者が`status`フィールドを手動で「満席」に切り替える運用とする。
定員数そのものをフィールドとして持つ必要もない(手動運用のため、数値管理はこのシステムの外で行われる)。
将来、システム連携の要否を含めて改めて検討する可能性はあるが、初回リリースのスコープには含めない。

## ライセンス

GPLv2以降。WordPress本体・WordPress公式プラグインディレクトリの慣習に合わせる。
将来的に公式ディレクトリでの配布を視野に入れるため、GPL互換を維持すること。

## リポジトリ方針

モノレポではなく分離構成(ライセンスが異なる可能性があるため)。NPO法人名義のGitHub組織リポジトリ内に配置する。

## 開発順序とMVP(決定事項)

3リポジトリの開発順序は `consultation-connector` → `consultation-hub` → `consultation-app`。
理由: このリポジトリは(WordPressでのWeb公開という)単独で価値が出る機能を持つため、
アプリ・中央管理が未完成でも先に動くものとして完成させられる。また、ここで
REST APIの実データ・挙動を先に固めることで、`consultation-app`側の手戻りを防ぐ。

### MVPに含めるもの
- カスタム投稿タイプ・入力UI(実装済み)
- REST API公開(実装済み)
- **Web表示(一覧・個別)** — 動的ブロックによる日程一覧表示(下記参照)、
  個別記事側は`the_content`フィルタでの開催情報自動追記(未実装)
- 予約リンクの表示(ステータスが「受付中」の場合のみ)

### MVPに含めないもの
- 予約フォーム・予約データの保持
- QRコード生成
- 定員のシステム連携
- `consultation-hub`への通知トリガー連携(hub完成後のフェーズ2で追加)

### Web表示の実装方式(決定事項)

FSE(サイトエディタ)対応が必須のため、**ショートコードではなく動的ブロック**
(`blocks/event-list/`、`block.json` + `render.php`によるサーバーサイドレンダリング)を採用する。
理由:
- ショートコードもFSE上で動作はするが、コアの「ショートコード」ブロック経由という回り道になり、
  サイトエディタからの編集体験(プレビュー、ブロック挿入UI)で劣る
- 実装コストはショートコードとほぼ同等
- WordPressの現行の推奨アプローチに沿う

個別記事側(投稿詳細ページ)の開催情報表示は、テーマのテンプレート対応をあてにしないため、
引き続き`the_content`フィルタでの自動追記方式とする(ブロックとは別立て、未実装)。

## 今後の拡張候補(初回リリースのスコープ外)

- QRコード生成機能のWordPress管理画面への組み込み
- 定員のシステム連携・自動満席判定
- 住所からの地図埋め込み表示(Googleマップ等)
